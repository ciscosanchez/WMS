# Page Review Checklist

The following pages were generated by AI agents and need manual browser review for UX, accessibility, and edge cases.

## Priority 1: Business-Critical Flows

- [ ] `/yard-dock` — Dashboard loads, stats cards show real data
- [ ] `/yard-dock/appointments/new` — Create appointment form submits, overlap validation works
- [ ] `/yard-dock/appointments/[id]` — Status action buttons transition correctly
- [ ] `/yard-dock/check-in` — Kiosk form submits, success/error states work
- [ ] `/yard-dock/calendar` — Gantt chart renders with real appointments
- [ ] `/yard-dock/yard-map` — Grid renders spots, color-coding correct
- [ ] `/labor` — Dashboard KPIs populated after shift + tasks
- [ ] `/shift` (operator) — Clock in/out flow, break buttons, AlertDialog works
- [ ] `/returns` — RMA list renders, status badges correct
- [ ] `/returns/new` — Create form with dynamic lines, client/product selects load
- [ ] `/returns/[id]` — Status action buttons work through full lifecycle
- [ ] `/inspect` (operator) — Inspection queue shows RMAs, inspect form submits
- [ ] `/inspect/[id]` — Disposition per line, finalize button appears when done

## Priority 2: Setup/Config Pages

- [ ] `/shipping/carton-types` — CRUD table, delete works
- [ ] `/products/kits` — Kit list, detail page shows components
- [ ] `/shipping/cross-dock` — Rules + plans tables render
- [ ] `/shipping/compliance` — Compliance checks, hazmat flags display
- [ ] `/warehouse/automation` — Device registry, status dots, dialog form
- [ ] `/inventory/slotting` — Run triggers BullMQ job, results appear
- [ ] `/inventory/slotting/[runId]` — Accept/reject/bulk/execute buttons work
- [ ] `/inventory/slotting/settings` — Config saves and persists

## Priority 3: Analytics/Reports

- [ ] `/analytics` — All 6 charts render (may need data to verify)
- [ ] `/labor/shifts` — Shift history table with duration calculations
- [ ] `/labor/costs` — Cost report with date range filtering
- [ ] `/interleave` (operator) — Route build, step execution, completion flow

## Known Gaps to Check

- Select components may need `onValueChange` null handling (Base UI vs Radix API)
- AlertDialog trigger may need `render` prop instead of `asChild` (Base UI)
- Date formatting may show UTC instead of tenant timezone
- Empty states should show when no data exists
- Error toasts should fire on all server action failures
- Loading states should appear during async operations
